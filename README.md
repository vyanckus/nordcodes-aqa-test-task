# Автоматизированные тесты для Spring Boot приложения (AQA Test Task)

![Java CI with Allure Report](https://github.com/vyanckus/nordcodes-aqa-test-task/actions/workflows/ci.yml/badge.svg)

Проект содержит автоматизированные end-to-end тесты для Spring Boot приложения, которое взаимодействует с внешним сервисом.
Тесты написаны на Java с использованием JUnit 5, WireMock, RestAssured и Allure для отчётности.

## Структура проекта

```
src/test/java/com/nordcodes/aqa/  
├── 📁 config/ # Конфигурационные классы  
│    ├── TestConfig.java # Константы (URL, заголовки, коды ответов)  
│    └── WireMockConfig.java # Управление WireMock-сервером  
├── 📁 client/ # HTTP-клиент для тестируемого приложения  
│    └── AppClient.java  
├── 📁 mock/ # Моки внешнего сервиса  
│    └── ExternalServiceMock.java  
├── 📁 tests/ # Тестовые классы  
│    └── AppEndpointE2ETest.java  
│    └── AppEndpointHexE2ETest.java 
└── 📁 utils/ # Вспомогательные утилиты  
     ├── TokenGenerator.java # Генератор токенов  
     └── AllureAttachments.java # Утилиты для Allure-аттачментов
```


## Предварительные требования

- **Java 17** или выше
- **Maven 3.6+**
- **Тестируемое приложение** (скачать по ссылке из ТЗ, пароль архива: `g7%Kp9#rX2bL`)

## Настройка окружения

1. **Запустите тестируемое приложение** (Spring Boot сервис):
   ```bash
   java -jar -Dsecret=qazWSXedc -Dmock=http://localhost:8888/ internal-0.0.1-SNAPSHOT.jar
   ```
   
Приложение будет доступно на `http://localhost:8080`.

2. **Убедитесь, что порты свободны:**


*   `8080` — тестируемое приложение

*   `8888` — WireMock (запускается автоматически перед тестами)


## Запуск тестов

### Общий запуск с пропуском AspectJ
Из-за особенностей настройки Allure для корректного запуска необходимо отключать AspectJ:

```bash
mvn clean test -Daspectj.skip=true
```

### Запуск тестов по ТЗ (A-Z0-9 токены)

Тесты проверяют соответствие приложения предоставленной спецификации:

```bash

mvn test -Dtest=AppEndpointE2ETest -Daspectj.skip=true
```

### Запуск тестов фактического поведения (HEX-токены 0-9A-F)

Тесты проверяют реальную бизнес-логику приложения:

```bash

mvn test -Dtest=AppEndpointHexE2ETest -Daspectj.skip=true
```

### Генерация и просмотр Allure-отчёта

```bash

mvn allure:report -Daspectj.skip=true
mvn allure:serve
```

## Два набора тестов

Проект содержит два набора тестов, отражающие разные подходы к тестированию:

### 1\. Тесты по ТЗ (`AppEndpointE2ETest`)

*   Цель: Проверить соответствие приложения предоставленной спецификации.

*   Особенность: Используют токены формата A-Z0-9 (как указано в ТЗ).

*   Результат: Часть тестов падает, что корректно фиксирует расхождения между ТЗ и реализацией (см. раздел "Найденные несоответствия").


### 2\. Тесты фактического поведения (`AppEndpointHexE2ETest`)

*   Цель: Проверить реальную бизнес-логику приложения с корректными входными данными.

*   Особенность: Используют HEX-токены (0-9A-F), которые фактически ожидает приложение.

*   Результат: Все тесты проходят, подтверждая, что основная логика (аутентификация, действия, управление сессией) работает корректно.

## Покрытие сценариев

Тесты в проекте разделены на два набора, каждый из которых проверяет систему с разной точки зрения.

### Тесты по ТЗ (`AppEndpointE2ETest`)
Проверяют соответствие приложения **предоставленной спецификации**. Используют токены формата **A-Z0-9**, как указано в ТЗ.

| Сценарий | Статус | Описание |
|----------|--------|----------|
| **LOGIN с токеном по ТЗ (A-Z0-9)** | 🔴 Падает | Выявляет ключевое расхождение: приложение ожидает HEX-формат (0-9A-F) и возвращает `400 Bad Request` с соответствующим сообщением. |
| **ACTION после LOGIN** | 🔴 Падает | Не выполняется, так как предварительный LOGIN не проходит из-за неверного формата токена. |
| **LOGOUT после LOGIN** | 🔴 Падает | Не выполняется, так как LOGIN не проходит из-за неверного формата токена. |
| **Повторный LOGIN** | 🔴 Падает | Не выполняется, так как LOGIN не проходит из-за неверного формата токена. |
| **LOGIN с ошибкой внешнего сервиса (400)** | 🔴 Падает | Приложение не доходит до вызова внешнего сервиса, так как токен (A-Z0-9) не проходит внутреннюю валидацию. |
| **ACTION без предварительного LOGIN** | ✅ Проходит | Приложение корректно отклоняет запрос с ошибкой `400`, запрос к `/doAction` не отправляется. |
| **LOGIN с токеном неверной длины (31 символ)** | ✅ Проходит | Приложение валидирует длину и возвращает ошибку `400` без вызова `/auth`. |
| **LOGIN с токеном недопустимых символов** | ✅ Проходит | Приложение валидирует символы и возвращает ошибку `400` без вызова `/auth`. |
| **Отправка запроса без X-Api-Key** | ✅ Проходит | Приложение валидирует обязательный заголовок и возвращает ошибку `400`. |
| **Неверный action (например, 'INVALID')** | ✅ Проходит | Приложение валидирует параметр `action` и возвращает ошибку `400`. |
| **Отсутствие параметра token** | ✅ Проходит | Приложение валидирует обязательный параметр и возвращает ошибку `400`. |
| **Отсутствие параметра action** | ✅ Проходит | Приложение валидирует обязательный параметр и возвращает ошибку `400`. |
| **Неверный Content-Type** | ✅ Проходит | Приложение валидирует заголовок и возвращает ошибку `400`. |
| **Таймаут внешнего сервиса** | ⏳ Отключён | Тест помечен `@Disabled` как пример проверки расширенного сценария. |

### Тесты фактического поведения (`AppEndpointHexE2ETest`)
Проверяют **реальную бизнес-логику** приложения. Используют **корректные HEX-токены (0-9A-F)**, которые фактически ожидает приложение.

| Сценарий | Статус | Описание |
|----------|--------|----------|
| **Успешный LOGIN с HEX-токеном** | ✅ Проходит | Приложение принимает валидный HEX-токен, вызывает `/auth` и возвращает `200 OK`. |
| **Успешный ACTION после LOGIN** | ✅ Проходит | После успешной аутентификации приложение разрешает действие, вызывает `/doAction` и возвращает `200 OK`. |
| **Успешный LOGOUT после LOGIN** | ✅ Проходит | Приложение корректно завершает сессию и возвращает `200 OK`. |
| **Повторный LOGIN с активным токеном** | ✅ Проходит | **Выявлено новое поведение:** приложение возвращает `409 Conflict` (не `200`), повторный вызов `/auth` не происходит. |
| **LOGIN: внешний сервис возвращает 400** | ✅ Проходит | Приложение получает ошибку от внешнего сервиса и возвращает `{"result":"ERROR"}`. |
| **LOGIN: внешний сервис возвращает 500** | ✅ Проходит | Приложение получает внутреннюю ошибку сервиса и возвращает `{"result":"ERROR"}`. |

**Условные обозначения статусов:**
- ✅ **Проходит** — тест выполняется успешно, проверяемое поведение подтверждено.
- 🔴 **Падает** — тест не проходит, так как выявляет **расхождение между ТЗ и реализацией**. Это ожидаемый и информативный результат для набора тестов по ТЗ.
- ⏳ **Отключён** — тест деактивирован (`@Disabled`), оставлен как пример для будущего расширения покрытия.


## 🚨 Найденные несоответствия (расхождения между ТЗ и реализацией)

В ходе тестирования выявлены следующие расхождения между предоставленной спецификацией и фактическим поведением приложения:

| Аспект | Ожидание по ТЗ | Фактическое поведение приложения | Объяснение и влияние |
|--------|----------------|----------------------------------|----------------------|
| **Формат токена** | Строка длиной 32 символа, состоящая только из **A-Z0-9** (заглавные буквы и цифры). | Токен должен соответствовать регулярному выражению `^[0-9A-F]{32}$`, т.е. содержать только **цифры 0-9 и заглавные буквы A-F** (HEX-формат). | **Влияние:** Все тесты, основанные на ТЗ и использующие токены с буквами G-Z, получают ошибку `400` с сообщением о несоответствии формату. |
| **Код ответа при ошибке валидации** | В ТЗ указано только тело ошибки (`{"result":"ERROR"}`), код ответа не специфицирован. | Приложение возвращает **HTTP-статус `400 Bad Request`** при ошибке валидации токена или параметров. | Соответствует общепринятой практике REST API. |
| **Повторный LOGIN с тем же токеном** | Подразумевается успешный ответ `200 OK` (перезапись сессии). | Приложение возвращает **`409 Conflict`** и **не обращается** к внешнему сервису `/auth` повторно. | Логика сессии строже: нельзя создать две активные сессии с одним токеном. Приложение проверяет внутреннее хранилище до вызова внешнего сервиса. |

**Рекомендация:** Уточнить у разработчиков или аналитиков корректный формат токена и поведение при повторном LOGIN, после чего обновить документацию.

## Технологический стек

*   **Java 17**

*   **JUnit 5** — фреймворк для тестирования

*   **WireMock** — мокирование внешнего сервиса

*   **RestAssured** — отправка HTTP-запросов

*   **Allure** — визуализация отчётов

*   **SLF4J + Logback** — логирование

*   **Maven** — управление зависимостями и сборкой


## Allure-отчёт

Allure-отчёт включает:

*   Детализацию шагов тестов (`@Step`)

*   Аттачменты запросов/ответов

*   Группировку тестов по фичам/стори (`@Epic`, `@Feature`, `@Story`)

*   Логи выполнения


## Расширение тестов

### Добавление нового теста

1.  Создайте метод в `AppEndpointE2ETest` с аннотацией `@Test`.

2.  Используйте `AppClient` для отправки запросов.

3.  Используйте `ExternalServiceMock` для настройки стабов.

4.  Добавьте проверки через `Assertions.assert...`.


### Добавление нового стаба WireMock

1.  Расширьте `ExternalServiceMock` новым методом.

2.  Используйте `stubFor(...)` для определения поведения.


## 👨‍💻 Автор

Фёдор Вянцкус

*   GitHub: [@vyanckus](https://github.com/vyanckus)

*   Email: vyanckus@mail.ru

* * *

Проект выполнен в рамках тестового задания на вакансию AQA Engineer в NordCodes.
